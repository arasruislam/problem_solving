#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    int calculation = (a * b) - (c * d);
    cout << "DIFERENCA = " << calculation << endl;

    return 0;
}